NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Admin dashboard (set in Vercel). Sign in at /admin/login. The session
# cookie is signed with a key derived from ADMIN_PASSWORD, so changing the
# password also signs every active session out.
ADMIN_EMAIL=
ADMIN_PASSWORD=
